# Core app and framework
fastapi
uvicorn[standard]

# Environment management
python-dotenv

# Database (PostgreSQL)
psycopg[binary]

# Vector store / memory
# Use ChromaDB < 0.5.0 to avoid tenant/database complexity
chromadb>=0.4.0,<0.5.0

# LLM client (Azure OpenAI compatible)
openai

# Visualization and data handling (used by VisualizeDataTool)
pandas
matplotlib
plotly
