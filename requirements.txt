# Core app and framework
fastapi
uvicorn[standard]

# Environment management
python-dotenv

# Database (PostgreSQL)
psycopg[binary]

# Vector store / memory
chromadb>=0.4.0

# LLM client (Azure OpenAI compatible)
openai

# Visualization and data handling (used by VisualizeDataTool)
pandas
matplotlib
plotly
